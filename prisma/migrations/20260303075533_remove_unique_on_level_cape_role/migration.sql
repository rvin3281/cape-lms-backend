BEGIN TRY

BEGIN TRAN;

-- DropIndex
ALTER TABLE [dbo].[cape_roles] DROP CONSTRAINT [cape_roles_level_key];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
